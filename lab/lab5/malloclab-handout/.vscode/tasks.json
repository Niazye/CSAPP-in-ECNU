{
    "tasks": [
        {
            "type":"shell",
            "label":"make",
            "command":"make clean && make",
            "args":[],
            "options": {
                "cwd": "${workspaceFolder}"
            }
        }
    ],
    "version": "2.0.0"
}